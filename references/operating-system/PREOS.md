# PREOS Source Operating System

The complete authoritative source document is the source-package member `production_risk_economics_evolution_operating_system.md`. The exact original package is stored as checksum-verified base64 chunks under `source-package/package-chunks/` and reconstructed byte-for-byte by PREOS.

Use:

```bash
python scripts/extract-source.py production_risk_economics_evolution_operating_system.md
```

PREOS deliberately keeps one immutable reconstructed package rather than a second editable long-form copy that can drift. `SOURCE-CONCEPT-COVERAGE.md` is the semantic coverage ledger for every operating-system concept integrated into the executable PREOS layer. It does not replace the authoritative source text.
