interface:
  display_name: "PREOS Project Init"
  short_description: "PREOS Project Init stage of integrated PREOS assurance"
  default_prompt: "Use $preos-project-init within the AI Product Delivery Blueprint + PREOS + gstack + Codex system. Preserve human authority, evidence semantics, selective loading, and approved scope."
policy:
  allow_implicit_invocation: true
